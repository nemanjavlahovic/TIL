# Backend

- [Feed Caching: Split the Shared Query from Per-User Personalization](feed-caching-strategy-split-cache-from-personalization.md) — Cache the expensive DB query, compute cheap per-user overlays live
- [PostgreSQL indexes are B-tree by default](postgresql-indexes-are-btree-by-default.md) — Every index and unique constraint creates a B-tree; add them to columns you filter/sort on
